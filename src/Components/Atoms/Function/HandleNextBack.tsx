export function handleNext(onNext: any) {
  if (onNext) {
    onNext()
  }
}

// Function to handle back action
export function handleBack(onBack: any) {
  if (onBack) {
    onBack()
  }
}

// ExportinghhandleNext and handleBack functions
